# polydioms-api
A card game for language learners to discover and practice idioms across multiple languages - API

```bash
netlify dev

netlify env:set DATABASE_URL "YOUR_NEON_CONNECTION_STRING"

netlify deploy --prod
```